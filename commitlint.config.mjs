/**
 * Conventional Commits linting.
 * https://www.conventionalcommits.org/
 *
 * Recommended scopes (optional): admin, client, api, shared-ui, data-access,
 * util, docker, ci, deps, repo.
 */
export default {
  extends: ['@commitlint/config-conventional'],
};
